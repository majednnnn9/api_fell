<!DOCTYPE html>
<html dir="rtl" lang="ar">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>نظام الأرشفة الجامعي</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        :root {
            --primary-color: #3b82f6;
            --secondary-color: #1e40af;
            --sidebar-width: 280px;
            --transition-speed: 0.3s;
        }

        body {
            background-color: #f1f5f9;
            overflow-x: hidden;
        }

        .sidebar {
            position: fixed;
            right: 0;
            top: 0;
            width: var(--sidebar-width);
            height: 100vh;
            background: linear-gradient(180deg, #1e293b 0%, #0f172a 100%);
            padding: 20px;
            color: white;
            transition: var(--transition-speed);
            z-index: 1000;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 40px;
            text-align: center;
            background: linear-gradient(45deg, #60a5fa, #3b82f6);
            padding: 15px;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.2);
            animation: logoGlow 2s infinite alternate;
        }

        .menu-item {
            padding: 15px;
            margin: 8px 0;
            border-radius: 10px;
            cursor: pointer;
            transition: all var(--transition-speed);
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .menu-item i {
            width: 24px;
            text-align: center;
        }

        .menu-item:hover {
            background-color: var(--primary-color);
            transform: translateX(-5px);
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.2);
        }

        .main-content {
            margin-right: var(--sidebar-width);
            padding: 30px;
            transition: var(--transition-speed);
        }

        .header {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
            margin-bottom: 30px;
            animation: slideDown 0.5s ease-out;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .header-profile {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .profile-image {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: var(--primary-color);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
        }

        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
            transition: all var(--transition-speed);
            animation: slideUp 0.5s ease-out;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        }

        .stat-card h3 {
            color: #64748b;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .stat-card .value {
            font-size: 28px;
            font-weight: bold;
            color: #1e293b;
            margin-bottom: 10px;
        }

        .stat-card .trend {
            display: flex;
            align-items: center;
            gap: 5px;
            font-size: 14px;
            color: #22c55e;
        }

        .chart-container {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
            animation: slideUp 0.7s ease-out;
        }

        .chart-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .chart-filters {
            display: flex;
            gap: 10px;
        }

        .filter-btn {
            padding: 8px 15px;
            border: none;
            border-radius: 8px;
            background: #f1f5f9;
            cursor: pointer;
            transition: var(--transition-speed);
        }

        .filter-btn:hover {
            background: var(--primary-color);
            color: white;
        }

        @keyframes slideDown {
            from {
                transform: translateY(-20px);
                opacity: 0;
            }

            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        @keyframes slideUp {
            from {
                transform: translateY(20px);
                opacity: 0;
            }

            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        @keyframes logoGlow {
            from {
                box-shadow: 0 4px 15px rgba(59, 130, 246, 0.2);
            }

            to {
                box-shadow: 0 4px 25px rgba(59, 130, 246, 0.4);
            }
        }

        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(100%);
            }

            .sidebar.active {
                transform: translateX(0);
            }

            .main-content {
                margin-right: 0;
            }

            .menu-toggle {
                display: block;
                position: fixed;
                right: 20px;
                top: 20px;
                z-index: 1001;
                background: var(--primary-color);
                color: white;
                padding: 10px;
                border-radius: 8px;
                cursor: pointer;
            }
        }

        .quick-actions {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .action-card {
            background: var(--primary-color);
            color: white;
            padding: 20px;
            border-radius: 15px;
            cursor: pointer;
            transition: all var(--transition-speed);
            text-align: center;
        }

        .action-card:hover {
            transform: translateY(-5px);
            background: var(--secondary-color);
        }

        .action-card i {
            font-size: 24px;
            margin-bottom: 10px;
        }

        .recent-items {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
            margin-bottom: 30px;
        }

        .item-list {
            border-collapse: collapse;
            width: 100%;
        }

        .item-list th,
        .item-list td {
            padding: 12px;
            text-align: right;
            border-bottom: 1px solid #e2e8f0;
        }

        .item-list th {
            background-color: #f8fafc;
            font-weight: bold;
        }

        .status-badge {
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 12px;
        }

        .status-available {
            background-color: #dcfce7;
            color: #166534;
        }

        .status-borrowed {
            background-color: #fee2e2;
            color: #991b1b;
        }

        .search-bar {
            background: white;
            padding: 15px;
            border-radius: 10px;
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }

        .search-bar input {
            flex: 1;
            padding: 10px;
            border: 1px solid #e2e8f0;
            border-radius: 5px;
        }

        .search-bar button {
            background: var(--primary-color);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
        }

        /* [باقي الأنماط CSS السابقة] */
    </style>
</head>

<body>
    <div class="menu-toggle" onclick="toggleSidebar()">
        <i class="fas fa-bars"></i>
    </div>

    <div class="sidebar">
        <div class="logo">
            <i class="fas fa-book"></i> المكتبة الجامعية
        </div>
        <div class="menu-item" style="background-color: var(--primary-color);
        transform: translateX(-5px);
        box-shadow: 0 4px 15px rgba(59, 130, 246, 0.2);
        ">
            <i class="fas fa-home"></i>
            <span>الرئيسية</span>
        </div>
        <div class="menu-item">
            <i class="fas fa-book"></i>
            <span>الكتب</span>
        </div>
        <div class="menu-item">
            <i class="fas fa-file-alt"></i>
            <span>أسئلة سابقة</span>
        </div>
        <div class="menu-item">
            <i class="fas fa-graduation-cap"></i>
            <span>مشاريع التخرج</span>
        </div>
        <div class="menu-item">
            <i class="fas fa-users"></i>
            <span>إدارة المستعيرين</span>
        </div>
        <div class="menu-item">
            <i class="fas fa-chart-bar"></i>
            <span>التقارير</span>
        </div>
        <div class="menu-item">
            <i class="fas fa-cog"></i>
            <span>الإعدادات</span>
        </div>
    </div>

    <div class="main-content">
        <div class="header">
            <h1>نظام الأرشفة الجامعي</h1>
            <div class="header-profile">
                <span>19 يناير 2025</span>
                <div class="profile-image">
                    <i class="fas fa-user"></i>
                </div>
            </div>
        </div>

        <div class="search-bar">
            <input type="text" placeholder="ابحث عن كتاب، مشروع تخرج، أو أسئلة سابقة...">
            <button><i class="fas fa-search"></i> بحث</button>
        </div>

        <div class="stats-container">
            <div class="stat-card">
                <h3>
                    <i class="fas fa-book"></i>
                    الكتب
                </h3>
                <div class="value">3,250</div>
                <div class="trend">
                    <i class="fas fa-plus"></i>
                    50 كتاب جديد هذا الشهر
                </div>
            </div>
            <div class="stat-card">
                <h3>
                    <i class="fas fa-file-alt"></i>
                    أسئلة سابقة
                </h3>
                <div class="value">1,840</div>
                <div class="trend">
                    <i class="fas fa-plus"></i>
                    120 مجموعة أسئلة
                </div>
            </div>
            <div class="stat-card">
                <h3>
                    <i class="fas fa-graduation-cap"></i>
                    مشاريع التخرج
                </h3>
                <div class="value">756</div>
                <div class="trend">
                    <i class="fas fa-plus"></i>
                    25 مشروع جديد
                </div>
            </div>
            <div class="stat-card">
                <h3>
                    <i class="fas fa-users"></i>
                    المستعيرين النشطين
                </h3>
                <div class="value">428</div>
                <div class="trend">
                    <i class="fas fa-arrow-up"></i>
                    نشط اليوم
                </div>
            </div>
        </div>

        <div class="quick-actions">
            <div class="action-card">
                <i class="fas fa-plus-circle"></i>
                <h3>إضافة كتاب جديد</h3>
            </div>
            <div class="action-card">
                <i class="fas fa-upload"></i>
                <h3>رفع أسئلة سابقة</h3>
            </div>
            <div class="action-card">
                <i class="fas fa-project-diagram"></i>
                <h3>إضافة مشروع تخرج</h3>
            </div>
        </div>

        <div class="recent-items">
            <h2>آخر الإضافات</h2>
            <table class="item-list">
                <thead>
                    <tr>
                        <th>العنوان</th>
                        <th>النوع</th>
                        <th>القسم</th>
                        <th>تاريخ الإضافة</th>
                        <th>الحالة</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>مقدمة في الذكاء الاصطناعي</td>
                        <td>كتاب</td>
                        <td>علوم الحاسب</td>
                        <td>2025/01/18</td>
                        <td><span class="status-badge status-available">متاح</span></td>
                    </tr>
                    <tr>
                        <td>نظم قواعد البيانات - 2024</td>
                        <td>أسئلة سابقة</td>
                        <td>نظم المعلومات</td>
                        <td>2025/01/17</td>
                        <td><span class="status-badge status-available">متاح</span></td>
                    </tr>
                    <tr>
                        <td>تطبيق إدارة المستشفيات</td>
                        <td>مشروع تخرج</td>
                        <td>هندسة البرمجيات</td>
                        <td>2025/01/15</td>
                        <td><span class="status-badge status-borrowed">معار</span></td>
                    </tr>
                </tbody>
            </table>
        </div>

    </div>

    <script>
        function toggleSidebar() {
            document.querySelector('.sidebar').classList.toggle('active');
        }

        // إضافة تأثير hover للبطاقات
        document.querySelectorAll('.stat-card, .action-card').forEach(card => {
            card.addEventListener('mouseover', () => {
                card.style.transform = 'translateY(-5px)';
            });
            card.addEventListener('mouseout', () => {
                card.style.transform = 'translateY(0)';
            });
        });
    </script>
</body>

</html>